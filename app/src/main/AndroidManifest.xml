<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.g015c1153.aid">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:name=".AidApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- android:networkSecurityConfig="@xml/network_security_config" -->

        <activity android:name=".SendActivity" />
        <activity android:name=".SentActivity" />
        <activity android:name=".SignUpForm" />
        <activity android:name=".SignUpCheck" />
        <activity android:name=".MemberAdd" />
        <activity android:name=".TeamAddActivity" />
        <activity android:name=".MemberList" />
        <activity android:name=".UserCalendar"/>
        <activity android:name=".TeamCalendar"/>
        <activity android:name=".GameRegistration"/>
        <activity android:name=".PracticeRegistration"/>
        <activity android:name=".DataGame"/>
        <activity android:name=".GameResult"/>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BORROWABLE" />

                <data
                    android:host="app"
                    android:scheme="login_kari" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TopActivity"
            android:label="@string/title_activity_top"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity
            android:name=".TeamPageActivity"
            android:label="@string/title_activity_team_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <uses-library android:name="org.apache.http.legacy" android:required="false"/>
    </application>

</manifest>